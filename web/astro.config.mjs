// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://lowtidebuild.github.io',
  base: '/podcast-briefing',
  output: 'static',
});
